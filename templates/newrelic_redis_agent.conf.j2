# vim:filetype=upstart
# {{ ansible_managed }}

description "New Relic Redis agent"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 3 30

exec {{ newrelic_redis_agent_binary_path }} -c {{ newrelic_redis_agent_conf_path }}
